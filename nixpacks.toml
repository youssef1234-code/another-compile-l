[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
dependsOn = ["setup"]
cmds = [
  "cd backend",
  "npm ci"
]

[phases.build]
dependsOn = ["install"]
cmds = [
  "cd backend",
  "npm run build"
]

[start]
cmd = "cd backend && npm start"
