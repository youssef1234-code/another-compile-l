# Backend Environment Variables for Vercel
# Copy and paste these into Vercel Dashboard → Environment Variables

NODE_ENV=production
PORT=5000
API_URL=https://another-compile-l-backend.vercel.app
CLIENT_URL=https://another-compile-l-frontend.vercel.app

# MongoDB Atlas (REQUIRED)
# Get from: https://cloud.mongodb.com/
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/event-manager?retryWrites=true&w=majority

# JWT Secrets (REQUIRED - generate strong random strings)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-min-32-characters-long-change-this
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-refresh-secret-min-32-characters-long-change-this
JWT_REFRESH_EXPIRES_IN=30d

# Email Configuration (REQUIRED for user verification)
# For Gmail: Use App Password from Google Account Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password
EMAIL_FROM=noreply@yourdomain.com

# Stripe Configuration (REQUIRED for payments)
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_live_your_stripe_publishable_key
# Webhook secret - get after creating webhook endpoint (Step below)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Security & App Settings
BCRYPT_ROUNDS=10
VERIFICATION_TOKEN_EXPIRES=24h
HOLD_MINUTES=15

# Optional: Disable auto-seeding on first run
# SEED_COMPREHENSIVE=false

# Admin Account (Default credentials for first admin)
# IMPORTANT: Change these after first login!
ADMIN_EMAIL=admin@guc.edu.eg
ADMIN_PASSWORD=Admin123!@#
